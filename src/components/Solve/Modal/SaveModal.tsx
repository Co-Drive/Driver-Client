import ModalPortal from './ModalPortal';
import SaveModalForm from './SaveModalForm';

const SaveModal = () => {
  return (
    <ModalPortal>
      <SaveModalForm />
    </ModalPortal>
  );
};

export default SaveModal;
